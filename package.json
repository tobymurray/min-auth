{
  "name": "min-auth",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/tobymurray/min-auth.git",
  "author": "Toby Murray <murray.toby@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start-server": "cd min-auth-server && yarn && nodemon",
    "start-client": "cd min-auth-client && yarn && ng serve --watch",
    "build-client": "cd min-auth-client && yarn && ng build --prod --aot && cd ..",
    "deploy-client": "yarn && node build.js",
    "build": "yarn build-client && yarn deploy-client && cd .."
  },
  "devDependencies": {
    "fs-extra": "^1.0.0"
  }
}
